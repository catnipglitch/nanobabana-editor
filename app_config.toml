# アプリケーション設定ファイル
# nanobanana-editor の全体設定

# UI設定
[ui]
# 起動時の初期タブ
# 利用可能な値: gemini, imagen, reference, agent, settings
# 注意: APIキー未設定時は自動的に settings が選択されます
default_tab = "gemini"

# プロンプトテンプレートは config/templates/ ディレクトリに移動しました
# - config/templates/text_to_image.toml: Text-to-Image テンプレート
# - config/templates/image_edit.toml: Image Edit テンプレート
